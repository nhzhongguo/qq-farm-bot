# 版本记录

本项目从 `v2.3.0` 开始使用语义化版本和不可移动的 Git 标签管理发布。

## v2.5.0 - 待发布（分批实施中）

### 新增

- 新增收益统计页：金币/经验/操作数 7/30/90 天趋势曲线（自绘 SVG，无新增依赖），基于每日统计历史自动归档（保留 90 天）。
- 告警推送打通 pushoo 19 种渠道：新增 qmsg、Server酱、PushPlus、钉钉、企业微信、Bark、Telegram、飞书、Discord 等推送渠道，支持在告警规则与"测试推送"中使用。
- 新增公告管理：管理员在管理后台编辑登录公告内容，可设置"仅展示一次"（用户已读后不再弹出），用户登录后弹窗展示。
- 新增卡密消费流水：记录卡密的注册激活、续费使用与领取操作，管理后台可视化查看、按类型过滤、支持清空。

### 性能

- JSON 存储写入合并（debounce）：审计日志、登录日志、卡密流水等高频追加型数据合并为一次原子写，进程退出时统一 flush 兜底。
- 作物排名计算加 5 分钟内存缓存（按排序键），减少重复解析与计算。
- WebSocket 日志推送节流：账号日志/系统日志按账号 200ms 合并为批量推送，前端兼容单条与批量两种 payload，减少 UI 渲染压力。

### 修复

- 修复 `user_info` 本地存储序列化问题：`useStorage('user_info', null)` 默认值 null 使 vueuse 退化为 raw 字符串序列化器，导致刷新页面后管理员角色丢失、`/admin` 被重定向回概览页。现显式使用 JSON 序列化并兼容历史脏数据。

### 测试

- 新增卡密流水、收益趋势、JSON 合并写入、作物排名缓存单元测试（共 130 项全部通过）。
- 新增 Playwright E2E：管理后台（告警/审计/公告/日志/收益统计）5 用例、认证冒烟 5 用例、调度页移动端 1 用例（共 11 项全部通过）。

## v2.4.0 - 2026-07-19

### 新增

- 新增操作审计日志：记录关键管理操作，敏感字段（密码/令牌/卡密/API Key 等）自动脱敏，90 天保留、2000 条上限，支持按操作者/动作/级别过滤查询。
- 新增失败诊断包：一键生成账号状态、任务记录、配置摘要的脱敏 JSON 诊断包，支持导出下载，7 天保留。
- 新增任务级重试策略：指数退避 + 随机抖动，可识别超时/5xx/429 等可重试错误，任务执行更稳定。
- 新增配置导入/导出与策略模板：14 个白名单字段导出、导入校验、命名模板保存与复用。
- 新增告警规则构建器：连续失败/离线时长/任务错误数 3 种条件，webhook/日志 2 种通道，最多 20 条规则，含触发记录。
- 新增多账号批量运维：批量启动/停止/重启，带权限校验与审计记录。
- 新增运行环境 Doctor 增强：数据目录、版本清单、Node 版本、内存、磁盘、资源路径、存储完整性、调度器快照等系统健康检查。
- 新增任务历史持久化：Worker 生命周期事件持久化到 `task-run-store`，前端调度页展示运行历史。

### 修复

- 修复默认管理员强制改密流程回归：测试环境不再绕过密码修改校验。
- 修复前端类型错误：Dashboard 管理判断、AdminPanel 告警开关、审计日志掩码断言。

### 安全

- 审计日志与诊断包统一敏感字段脱敏规则，不输出原始凭据。
- 诊断包导出禁止泄露本地绝对路径。

### 测试

- 新增审计日志、诊断包、重试策略、策略模板、告警规则引擎单元测试。
- 扩展 Runtime Doctor 与 HTTP 安全回归测试（共 109 项全部通过）。

## v2.3.2 - 2026-07-15

### 安全

- 登录/注册/卡密领取接口增加 IP 级 HTTP 限流（分别 20/10/10 次每分钟），超限返回 429。
- 增加基线 HTTP 安全响应头（`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy` 等），并关闭 `X-Powered-By`。
- 明确 `/api` 公共白名单：登录/注册、卡密领取、游戏版本、健康检查；其余接口统一要求 token。
- 限制 JSON 请求体大小为 1MB，降低异常大包风险。

### 依赖

- 微信代理请求改用 Node 内置 `fetch`，移除 `node-fetch` 依赖。

### 测试

- 新增 HTTP 安全与鉴权回归测试：公共路由、安全头、受保护路由 401、登录后鉴权通过、登录/注册/卡密领取限流 429。

## v2.3.1 - 2026-07-15

### 修复

- 强化用户/卡密 JSON 备份写入：主文件损坏时不再错误覆盖 `.bak`，成功写入后刷新最新可用副本。
- 修复无 `type` 字段的旧卡密无法被领取的问题。
- 账号列表 `accounts.json` 增加 `.bak` 备份与损坏自动恢复。

### 安全

- 密码哈希比较改为 `timingSafeEqual`，降低时序侧信道风险。

### 测试

- 扩展数据安全回归测试：损坏恢复、旧卡密领取、账号备份恢复、默认管理员校验。

## v2.3.0 - 2026-07-14

### 新增

- 新增用户、卡密和登录数据的原子写入与 `.bak` 自动恢复。
- 新增卡密领取预留机制和 IP、客户端组合限制。
- 新增用户账号归属迁移与删除联动。
- 新增数据安全回归测试。
- 新增统一版本、发布标签和无损回退规范。

### 修复

- 修复策略保存覆盖自动化配置。
- 修复微信好友申请启动检查失效。
- 修复账号备注越权修改。
- 修复默认密码未强制修改。
- 修复缺失图标和静态检查错误。
- 修复 Docker 健康检查访问受保护接口。

### 安全

- 微信代理接口改为登录后访问，并限制允许的动作。
- 微信代理 API Key 仅保存在后端，不再下发普通用户浏览器。
- 生产依赖已升级，安全审计无已知漏洞。

## v2.2.1 - 回退基线

- 正式版本流程启用前的最后一个稳定提交。
- 保留为 `v2.3.0` 的直接回退目标。
